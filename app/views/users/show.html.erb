<card>
  <user-profile
    :user="<%= @user.to_json %>"
    :snippets-count="<%= @user.snippets.size %>"
  />
</card>

<% @user.snippets.order(created_at: :desc).each do |snippet| %>
  <snippet-preview
    :key="<%= snippet.id %>"
    :snippet="<%= snippet.serialize(current_user).to_json %>">
  </snippet-preview>
<% end %>
