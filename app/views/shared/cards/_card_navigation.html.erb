<div class="bg-white overflow-hidden shadow rounded-lg mb-4 cursor-pointer hover:shadow-md transition"
  <% if defined?(id) %>
    id="<%= id %>"
  <% end %>
  data-controller="navigation"
  data-navigation-url-value="<%= url %>"
  data-action="click->navigation#navigate"
>
  <div class="px-4 py-5 sm:p-4">
    <%= yield %>
  </div>
</div>
