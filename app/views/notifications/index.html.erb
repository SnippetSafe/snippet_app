<div class="card--container">
  <% @notifications.each do |notification| %>
    <%= render partial: "notifications/#{notification.notifiable_type.parameterize}", locals: { notification: notification } %>
  <% end %>
</div>
